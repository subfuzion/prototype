# git config core.hooksPath=.githooks

# The following  might be a more obvious approach since `.gitconfig` is part
# of the repo.  Note: the `../.gitconfig` path is relative to `./.git/config`
# (the local .git config) where `include.path` is updated.

git config --local include.path ../.gitconfig
